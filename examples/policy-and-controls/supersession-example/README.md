# Supersession Example

This example shows a synthetic policy version chain where one policy version supersedes another.

Supersession is represented with fields on the records and with a relationship record. This keeps version history portable even when a downstream application stores versions differently.

